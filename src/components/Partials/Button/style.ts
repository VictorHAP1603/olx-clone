import styled from "styled-components";

export const Container = styled.button`
  padding: 10px 20px;
  border: 0;
  outline: 0;
  border-radius: 4px;

  transition: 0.4s;

  &:hover {
    filter: brightness(0.8);
  }
`;
